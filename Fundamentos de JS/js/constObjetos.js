const numeros = [1, 2, 3];

// El arreglo no puede ser re asignado pero si por elemento
// numeros = [4, 2, 3]; ERROR
numeros[0] = 4

// También se pueden agregar números
numeros.push(5)

console.log(numeros);